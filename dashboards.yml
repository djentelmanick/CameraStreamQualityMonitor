apiVersion: 1

providers:
  - name: "Camera Monitoring"
    orgId: 1
    folder: "Camera Monitoring"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
